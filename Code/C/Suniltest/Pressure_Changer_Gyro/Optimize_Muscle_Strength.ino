// --------------- Balancing --------------- //

// if IP is perfectly balanced, muscles can relax and just need to adjust time to time
  // muscles might be too jerky to adjust without other side being tensioned

  // maybe have a standby muscle tension so that they can jump in and adjust more quickly?

// if IP is leaning right, only left muscle needs to work to pull it back to balanced
  // right muscle needs to be on standby to adjust


// --------------- Moving With balance platform --------------- //

// 90 degrees has to be set correctly at the start

// closer to middle, both are tensioned
  // tensioning for left example:
  // left pressure:
    
